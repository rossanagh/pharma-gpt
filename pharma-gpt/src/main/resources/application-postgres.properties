# Profil opțional: doar port 8083 + import local (datele DB vin din application.properties).
# Pornește cu: SPRING_PROFILES_ACTIVE=postgres,dev
spring.config.import=optional:classpath:application-local.properties

server.port=${SERVER_PORT:8083}
